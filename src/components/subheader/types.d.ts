namespace Subheader {
    interface Props {
        text: string;
    }
    namespace Icon {
        interface Props {
            path: string;
            children: JSX.Element;
        }
    }
}

export default Subheader;
