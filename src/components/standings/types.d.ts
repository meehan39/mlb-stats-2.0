namespace Standings {
    namespace Row {
        interface Props {
            teamKey: string;
            teamName: string;
            topFour: number;
            total: number;
        }
    }
}

export default Standings;
